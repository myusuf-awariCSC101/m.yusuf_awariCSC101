fn main() {

	//using vector::new()
	let v : Vec<i64> = Vec::new();

	//printing the size of vector
	println!("\nThe length of the Vect::new is: {}",v.len());

	//using macro 
	let v = vec!["Grace", "Effing", "Basil", "Kareem", "Susan"];

    //printing the size of vector
    println!("\nThe length of vec macro is: {}",v.len())

	
}
